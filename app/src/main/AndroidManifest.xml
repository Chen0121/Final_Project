<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chen.final_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Patient_Intake_form"
            android:label="@string/intakeForm_Title" />
        <activity android:name=".movie_information" />
        <activity android:name=".multiple_choice_quiz_creater" />
        <activity android:name=".octranspo_bus_route_app" />
        <activity android:name=".intake_form_patient_list_retrival" />
        <activity
            android:name=".IntakeFormDoctorListView"
            android:label="@string/intakeForm_RegisterForDoctor" />
        <activity android:name=".Intake_Form_Add_Doctor_Patient"></activity>
    </application>

</manifest>